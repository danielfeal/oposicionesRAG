# Virtual environment
/.venv

# Secrets
/.env

# Python cache
**/__pycache__/

# CLAUDE.md
/CLAUDE.md

# Corpus of pdf and txt files
/corpus

#Chainlit
# config.toml and chainlit.md are deliberately NOT ignored.
# language is forced to "es" (config.toml), so only es.json needs to ship
/.chainlit/translations/*
!/.chainlit/translations/es.json
/.files/
